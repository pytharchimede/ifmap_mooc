<main class="customer-auth-shell"><a class="auth-logo customer-logo" href="/"><span>IF</span><div><strong>IFMAP</strong><small>ACTIVATION</small></div></a><div class="auth-form customer-form"><p class="eyebrow">VÉRIFICATION DU COMPTE</p><h2>Activez votre espace</h2><p>Entrez le code à 6 chiffres envoyé sur votre téléphone. Le code expire après 15 minutes.</p><?php if($flash): ?><div class="toast">✓ <?= htmlspecialchars($flash) ?></div><?php endif ?><?php if($error): ?><div class="auth-error">! <?= htmlspecialchars($error) ?></div><?php endif ?><?php if($demoOtp): ?><div class="demo-credentials"><span>Mode local — OTP</span><code><?= htmlspecialchars($demoOtp) ?></code></div><?php endif ?><form method="post" action="/activation"><label>Code OTP<input name="otp" inputmode="numeric" pattern="[0-9]{6}" maxlength="6" autocomplete="one-time-code" placeholder="000000" required autofocus></label><button class="auth-submit">Activer mon compte →</button></form><form method="post" action="/activation/renvoyer"><button class="auth-secondary" type="submit">Je n’ai pas reçu le code · Renvoyer</button></form><p class="auth-switch">Toujours rien ? L’administrateur peut activer manuellement votre compte.</p></div></main>
